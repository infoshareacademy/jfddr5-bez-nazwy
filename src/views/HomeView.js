import { useState } from "react";
import Header from "../components/Header/Header";

const HomeView = ({ setProduct, product, setCategory, setCity, city }) => {
	return <div>Hero section/content</div>;
};

export default HomeView;
